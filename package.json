{
  "name": "mobileapp-api",
  "version": "0.0.1",
  "private": true,
  "description": "NestJS API — uygulama kodu backend/ klasöründe (Hostinger framework tespiti)",
  "scripts": {
    "postinstall": "node scripts/install-backend.cjs",
    "build": "node scripts/install-backend.cjs",
    "start": "node scripts/hostinger-start.cjs",
    "start:prod": "node scripts/hostinger-start.cjs"
  },
  "dependencies": {
    "@nestjs/common": "^10.0.0",
    "@nestjs/core": "^10.0.0",
    "@nestjs/platform-express": "^10.0.0"
  },
  "engines": {
    "node": ">=18 <=22"
  }
}
